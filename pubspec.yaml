name: we_drive_partner
description: WE DRIVE Partner App - clean frontend master
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_messaging: ^15.2.10
  url_launcher: ^6.3.0
  image_picker: ^1.1.2
  firebase_storage: ^12.0.0
  geolocator: ^13.0.2
  razorpay_flutter: ^1.4.6
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
